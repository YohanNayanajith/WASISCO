import React from "react";

const WasiscoImageSection = ({ reference }) => {
  return (
    <section ref={reference}>
      <div className="main-section-cnt">WasiscoImageSection</div>
    </section>
  );
};

export default WasiscoImageSection;
